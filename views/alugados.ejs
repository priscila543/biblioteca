<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Meus Livros Alugados</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body style="background: linear-gradient(135deg, #ad7b4f, #6e320f); min-height: 100vh;" class="container py-5">

  <h1 class="text-center mb-4" style="color: black;">Meus Livros Alugados</h1>

  <% if (livros.length === 0) { %>
    <div class="alert alert-info text-center">
      Você ainda não possui livros alugados.
    </div>
    <div class="text-center">
      <a href="/inicio" style="background:black; color:white; border-color:black;" class="btn btn-warning">Voltar ao catálogo</a>
    </div>
  <% } else { %>

    <div class="row">
      <% livros.forEach(livro => { %>
        <div class="col-md-4 mb-4">
          <div class="card h-100 shadow">
            <img src="<%= livro.capa %>" class="card-img-top" style="height: 250px; object-fit: cover;">
            <div class="card-body">
              <h5 class="card-title"><%= livro.nome %></h5>
              <p class="card-text"><strong>Autor:</strong> <%= livro.autor %></p>
              <p class="card-text">
                <strong>Data de devolução:</strong><br>
                <span class="text-danger fw-bold"><%= livro.dataDevolucao %></span>
              </p>
              <form action="/devolver/<%= livro.id %>" method="POST">
                <button class="btn btn-danger w-100 mt-2">
                  Devolver Livro
                </button>
              </form>

            </div>
          </div>
        </div>
      <% }); %>
    </div>

    <div class="text-center mt-4">
      <a href="/inicio" class="btn btn-secondary">Voltar ao Início</a>
    </div>

  <% } %>

</body>
</html>
