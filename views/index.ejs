<!DOCTYPE html>
<html lang="pt-br">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>Início - Biblioteca Virtual</title>

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body style="background: linear-gradient(135deg, #ad7b4f, #6e320f); min-height: 100vh;">

<% var ultimos = (typeof ultimos !== "undefined" && ultimos) ? ultimos : []; %>

<header class="header-bar text-center">
  <br>
  <h2 style="color: black;">BIBLIOTECA VIRTUAL</h2>

  <div class="text-end me-3">
    Bem-vindo(a), <%= usuario.nome %> |
    <a href="/logout" style="background:black; color:white; border-color:black;" class="btn btn-warning">
      Sair
    </a>
  </div>

  <nav class="mt-2">

    <button 
      type="button"
      data-bs-toggle="modal" 
      data-bs-target="#cats"
      style="background:black; color:white; border-color:black;" 
      class="btn btn-warning">
      Categorias
    </button>

    <a href="/alugados" style="background:black; color:white; border-color:black;" class="btn btn-warning">
      Meus Livros Alugados
    </a>

    <% if (usuario.tipo === "admin") { %>
      <a href="/livros" style="background:black; color:white; border-color:black;" class="btn btn-warning">
        Cadastrar Livros
      </a>
    <% } %>
  </nav>
</header>

<div class="container">

  <form action="/buscar" method="GET" class="input-group mt-4">
    <input type="text" name="q" class="form-control" placeholder="Buscar livros..." required>
    <button class="btn btn-dark">Pesquisar</button>
  </form>

  <h4 style="color: black;" class="fw-bold text-center mt-5 mb-3">Últimos Livros Cadastrados</h4>

  <div class="row g-3">
    <% if (ultimos.length > 0) { %>
      <% ultimos.forEach(livro => { %>
        <div class="col-md-3">
          <div class="card sombra">
            <img src="<%= livro.capa %>" class="card-img-top">
            <div class="card-body text-center">
              <h6 class="fw-bold"><%= livro.nome %></h6>
              <small class="text-muted"><%= livro.autor %></small><br>
              <a href="/livros/<%= livro.id %>" class="btn btn-dark btn-sm mt-2">
                Ver mais
              </a>
            </div>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <p class="text-center text-light">Nenhum livro cadastrado ainda.</p>
    <% } %>
  </div>

</div>

<div class="modal fade" id="cats" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content sombra">
      <div style="background-color: #a35d35;" class="modal-header">
        <h5 style="color: black;" class="fw-bold">Categorias</h5>
        <button class="btn-close" data-bs-dismiss="modal"></button>
      </div>

      <div style="background-color: #a35d35;" class="modal-body">
        <a href="/romance" style="color: black;" class="d-block mb-2">Romance</a>
        <a href="/ficcao" style="color: black;" class="d-block mb-2">Ficção Científica</a>
        <a href="/infantil" style="color: black;" class="d-block mb-2">Infantil</a>
      </div>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
