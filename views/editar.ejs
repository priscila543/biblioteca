<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Editar Livro</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body style="background: linear-gradient(135deg, #ad7b4f, #6e320f); min-height: 100vh;">

  <div class="container py-5">

    <h1 class="text-center mb-4">Editar Livro</h1>

    <form 
      action="/livros/editar/<%= livro.id %>" 
      method="POST" 
      class="card p-4 mx-auto sombra" 
      style="max-width: 500px;"
    >

      <div class="mb-3">
        <label class="form-label">Nome:</label>
        <input 
          type="text" 
          name="nome" 
          value="<%= livro.nome %>" 
          class="form-control" 
          required
        >
      </div>

      <div class="mb-3">
        <label class="form-label">Autor:</label>
        <input 
          type="text" 
          name="autor" 
          value="<%= livro.autor %>" 
          class="form-control" 
          required
        >
      </div>

      <div class="mb-3">
        <label class="form-label">Capa (URL):</label>
        <input 
          type="text" 
          name="capa" 
          value="<%= livro.capa %>" 
          class="form-control"
        >
      </div>

      <div class="mb-4">
        <label class="form-label">Categoria:</label>
        <select name="categoria" class="form-select" required>
          <option value="romance" <%= livro.categoria === "romance" ? "selected" : "" %>>Romance</option>
          <option value="ficcao" <%= livro.categoria === "ficcao" ? "selected" : "" %>>Ficção Científica</option>
          <option value="infantil" <%= livro.categoria === "infantil" ? "selected" : "" %>>Infantil</option>
        </select>
      </div>

      <div class="d-flex justify-content-between">
        <a href="/livros" class="btn btn-secondary">
          ← Voltar
        </a>

        <button type="submit" class="btn btn-primary">
          Salvar Alterações
        </button>
      </div>

    </form>

  </div>

</body>
</html>
