<!DOCTYPE html>
<html lang="pt-br">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Login - Biblioteca</title>

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body style="background: linear-gradient(135deg, #ad7b4f, #6e320f); min-height: 100vh;" class="d-flex justify-content-center align-items-center vh-100">

<div 
  class="card p-4 text-center sombra" 
  style="
    width:350px;
    background: 
      linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)),
      url('https://images.unsplash.com/photo-1524995997946-a1c2e315a42f');
    background-size: cover;
    background-position: center;
    color: white;
    border: none;
  "
>

  <h2 class="titulo mb-3">BIBLIOTECA VIRTUAL</h2>
  <p class="titulo mb-3">Acesse sua conta</p>

  <% if (erro) { %>
      <div class="alert <%= erro.includes('sucesso') ? 'alert-success' : 'alert-danger' %>">
        <%= erro %>
      </div>
  <% } %>

  <form action="/login" method="POST">
    <div class="mb-3 text-start">
      <label class="fw-bold">E-mail</label>
      <input name="usuario" type="email" class="form-control" required>
    </div>

    <div class="mb-3 text-start">
      <label class="fw-bold">Senha</label>
      <input name="senha" type="password" class="form-control" required>
    </div>

    <button class="btn btn-dark w-100 mb-2">Entrar</button>
    <a href="/cadastro" class="btn btn-dark w-100 mb-2">Cadastrar</a>
  </form>

</div>

</body>
</html>
